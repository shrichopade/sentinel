# requirements.txt — Python dependencies for Sentinel.AI backend
# Install from repository root with:
#   pip install -r docs/requirements.txt
#
# Notes (Windows/local dev):
# - `supabase==2.7.4` is intentionally pinned because newer versions pulled in build-heavy
#   transitive dependencies on some Windows setups in earlier iterations of this project.
# - `PyMuPDF` is installed as "PyMuPDF" but imported in code as `fitz`.

# Web API framework + server
fastapi>=0.110
uvicorn[standard]>=0.23

# File upload parsing (FastAPI needs this for multipart/form-data)
python-multipart>=0.0.9

# Environment variables from backend/.env
python-dotenv>=1.0.1

# Scheduling (monitoring loop)
APScheduler>=3.10

# Supabase client + HTTP stack
supabase==2.7.4
httpx>=0.26
postgrest>=0.16

# Data validation (FastAPI uses pydantic models)
pydantic>=2.6

# LLM + embeddings
anthropic>=0.30
voyageai>=0.2

# Document parsing (PDF)
PyMuPDF>=1.24

# Email sending (Resend)
resend>=2.4

# Tests
pytest>=8.0
pytest-asyncio>=0.23

